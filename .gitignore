__pycache__/
*.pyc
*.pyo
.env
venv/
.venv/
.claude/
.pytest_cache/

# Generated reports from real device runs may contain device-specific
# findings; keep them local. sample_report.html (synthetic demo fixture)
# is intentionally NOT excluded, it's the portfolio artifact.
audit_report.html

# Real device credentials never get committed; hosts.yaml.example is the
# template that does.
inventory/hosts.yaml
